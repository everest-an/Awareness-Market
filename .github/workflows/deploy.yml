name: Deploy (Disabled)

# Disabled: replaced by deploy-backend.yml and ci-cd-unified.yml
on:
  workflow_dispatch:
  # push:
  #   branches: [main]

env:
  PNPM_VERSION: '10'
  API_BASE_URL: ${{ vars.VITE_API_URL || 'https://api.awareness.market' }}

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    environment: production-backend

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build application
        run: pnpm run build
        env:
          NODE_ENV: production

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /home/ec2-user/awareness-network
            git pull origin main
            pnpm install --prod
            pnpm run build
            pm2 restart awareness-network || pm2 start ecosystem.config.js

      - name: Health Check
        env:
          API_BASE_URL: ${{ vars.VITE_API_URL || 'https://api.awareness.market' }}
        run: |
          sleep 30
          curl -f "$API_BASE_URL/api/health" || exit 1
        continue-on-error: true

      - name: Notify on Success
        if: success()
        run: echo "Deployment successful!"

      - name: Notify on Failure
        if: failure()
        run: echo "Deployment failed!"
